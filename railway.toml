[build]
nixpacksVersion = "v1"

[build.nixpacks]
aptPkgs = []
nixPkgs = ["nodejs_24"]

[deploy]
startCommand = "npm start"
healthcheckPath = "/"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
